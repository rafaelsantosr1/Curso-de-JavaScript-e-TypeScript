// Tipos de dados primitivos
// String, number, undefined, null, boolean, symbol

const nome = 'Rafael'; //string
const nome1 = "Rafael"; //string
const nome2 = `Rafael`; //string
const num1 = 10; //number
const num2 = 10.52; //number
let nomeAluno; //undefinet -> não aponta para local nenhum na memória
const sobrenomeAluno = null; //Nulo -> não aponta para local nenhum na memória
const boolean = true; // Bollean = true, false (lógico)



console.log(nome);